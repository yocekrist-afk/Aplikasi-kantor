rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules: Allow all read/write since login was removed for revision
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
